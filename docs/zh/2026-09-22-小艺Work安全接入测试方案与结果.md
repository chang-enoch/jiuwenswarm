# 小艺 Work 安全接入测试方案与结果

测试日期：2026-09-22（UTC+8）。旧包轮次 10:15–10:29；新版 debug 蓝区请求复测 11:25–11:30。状态：已完成本轮可执行用例，三个接入点完整验收仍未完成。

此前定向自动化 361 项通过；旧包桌面 3 次会话保留原始结果。新版另完成 4 次独立会话：云端 ACCEPT 后读取、本地 ALLOW 异步上报、本地 DENY、以及本地 ASK 后用户手动拒绝，均符合权限与上报预期。取得 6 个真实 moderation-text 请求结果（4 个 toolInput、2 个 toolOutput），均为 ACCEPT、每个请求仅有一次开始和一次结果。尚未取得真实 REJECT、CLARIFY、同步失败回退或 toolSkill 的完整云端证据。

新版位于 desktop worktree 的 `release/security-debug-20260922/`，11:07 构建完成。下述旧包数据与新版复测分节记录；构建成功、模拟管道测试和真实蓝区结果不混用。

## 范围与判断标准

覆盖工具执行前（toolInput）、工具执行后（toolOutput）、Agent 技能安装前（toolSkill）。core 提供扩展，Swarm 采集与决策，desktop 组装业务 body、鉴权及云端调用。

区分四类证据：组件测试使用模拟响应；跨进程测试使用真实 Windows 命名管道和模拟云端；桌面实测检查真实交互及落盘；蓝区验收需要真实云端审核结果。前三类通过不能代替第四类。未进入目标接入点、没有结果日志、HTTP 202 或“没有报错”都不能算云端验收通过。

## 环境与基线

| 项目 | 工作区 / 基线 |
|---|---|
| Swarm | `C:/Users/newcs/.codex/worktrees/xiaoyi-behavior-security/jiuwenclaw` / `875681e34b6df809f9156e6b1d27ba072456b4e6` |
| core | `C:/Users/newcs/data/code/agent-core-br_0.1.16.post2.hotfix/.security` / `cffa8b40aae8825b05eb9c04e4c49ebc015b4dea` |
| desktop | `C:/Users/newcs/data/code/claw_desktop/.security` / `47f4366517f496a7cdd97d00177c0a1aaac20093` |
| 分支 | 三仓 `codex/xiaoyi-behavior-security`，含未提交实现 |
| 旧包 | `release/win-unpacked-debug/小艺Work-Debug.exe`，0.7.1，9 月 21 日构建 |
| 新版复测包 | `release/security-debug-20260922/win-unpacked/小艺Work-Debug.exe`，0.7.1，9 月 22 日构建；通过实际窗口路径核对 |
| 旧轮证据 | desktop worktree 的 `build/security-validation-20260922/` |
| 新版蓝区证据 | desktop worktree 的 `build/security-debug-blue-20260922/` |

开始测试时读取到当前用户 `xiaoyi_work_security.cloud_authorization.enabled=true`，`timeout_ms=4000`。保留用户现有设置。既有 debug 包缺少后来增加的逐请求诊断日志，源码测试与包验证应分别说明版本。

## 用例与预期

| ID | 场景 | 通过标准 | 验证方式 |
|---|---|---|---|
| T01 | 未命中特定规则 | opt-in 后为 UNDETERMINED，旧宿主行为不变 | core |
| T02 | 显式 ALLOW / DENY / ASK | 保留本地结果；外部 hook 不能覆盖 | core + Swarm |
| T03 | 显式规则的上报 | 请求为 report；不等待云端决定 | Swarm |
| T04 | 无法判定且云授权关闭 | report + 本地 ASK | Swarm |
| T05 | 无法判定且云授权开启 | 一次 authorize 兼作上报，不额外重复请求 | Swarm + desktop |
| T06 | ACCEPT / REJECT / CLARIFY | 分别允许 / 拒绝 / 按当前约定允许本次 | Swarm + desktop |
| T07 | 超时 / 异常 / 非法响应 | 返回 unavailable，端侧转 ASK | 三层 |
| T08 | 取消执行 | 取消传播，不误判允许 | core + Swarm |
| T09 | 输出上报 | 仅实际执行后上报，包含合法 None 结果 | core + Swarm |
| T10 | 被拒绝、被跳过的工具 | 无虚假的工具输出事件 | core + Swarm |
| T11 | skill 安装允许 / 拒绝 | 允许才复制及登记；拒绝不落盘 | 实际 SkillToolkit / SkillManager |
| T12 | skill 本地确认恢复 | 安装前中断；同意通过，拒绝保留旧文件；事件不重复 | Swarm |
| T13 | SkillNet 线程替换 | 拒绝或需确认时，原技能不被覆盖 | Swarm |
| T14 | core 中断传播 | 安装确认沿实际回调链传播为 ToolInterruptException | Swarm + core |
| T15 | 云端 body | businessId、isXiaoyiAPP、三种 textSource、questionText 类型正确 | desktop |
| T16 | 云端 HTTP | moderation-text 请求头、业务响应校验、脱敏日志 | 本机 HTTP 模拟端 |
| T17 | 内部命名管道鉴权 | 错误 token 拒绝，正确 token 到达行为处理器 | 真实 Windows 管道 |
| T18 | Python 到 desktop 跨进程 | 允许、拒绝、clarify、不可用四种决定一致 | 打包 Python + 真实管道 |
| T19 | 队列和去重 | 队列有界、重复事件不重复调用、冲突事件拒绝、退出取消 | desktop |
| T20 | 桌面本地对话 | 新建测试会话能够调用工具，记录权限卡与实际结果 | debug 蓝区实测 |
| T21 | Agent 安装工具可达性 | 工具实际暴露，能从对话进入安装前 hook | 源码 + 包 + 实际工具列表 |
| T22 | 蓝区行为云端验收 | 真实 toolInput/toolOutput/toolSkill 请求及响应可关联，授权在执行前生效 | 真实云端 |
| T23 | skill 附件 | 真实上传对象、URL/hash/size/body 按最终契约核验 | 契约定稿后 |
| T24 | 新包 UNDETERMINED → 云端 ACCEPT | 单次输入审核通过后才读取合成文件，输出上报也有真实结果 | debug 蓝区 |
| T25 | 新包本地 ALLOW | 工具不等待输入审核；输入和输出各上报一次 | debug 蓝区 |
| T26 | 新包本地 DENY | 云端上报 ACCEPT 不覆盖端侧拒绝；没有虚假输出上报 | debug 蓝区 |
| T27 | 新包本地 ASK 后拒绝 | 上报不替代权限卡；用户拒绝后不执行、不重复输入上报、不生成输出上报 | debug 蓝区 + 用户手动选择 |

## 执行步骤

1. 固定三仓基线，记录包摘要与实际配置，只使用合成数据和测试会话。
2. 运行 core 安全模块与 AbilityManager 定向回归；Swarm 行为/安装测试；原有 SkillToolkit 回归。
3. 运行 desktop 行为、管道、共用风控、运行时和构建形态回归与 typecheck，保存日志及 JUnit。
4. 使用包内 Python 执行真实 Windows 命名管道集成测试；云端决定仍为模拟值。
5. 核对已运行包与当前源码的差异。桌面实测先查看当前会话状态，再创建独立测试会话。
6. 实测本地明确 ALLOW 的 todo_list，以及未命中特定规则的 read_file（仅合成 fixture）。出现权限卡由用户决定，本轮不保存永久授权。
7. 根据请求日志、界面结果和文件状态记录问题；所有无法验证项保留为阻塞或待验收。

安装组件自测脚本：`scripts/test_xiaoyi_skill_install.ps1`。最小技能与使用说明见同目录《2026-09-22-技能安装安全自测》。

## 本轮执行结果

### 自动化与包内验证

以下均为本轮重新执行的结果，未使用历史数量代替。证据目录：`C:/Users/newcs/data/code/claw_desktop/.security/build/security-validation-20260922/`。

| 验证 | 本轮结果 | 证据文件 |
|---|---|---|
| core 安全模块 + AbilityManager 两组定向回归 | 218 passed；0 failed；2 条已有 Pydantic 弃用告警 | `core.log` / `core.xml` |
| Swarm 行为上报、授权和技能安装 | 18 passed；0 failed | `swarm-behavior.log` / `swarm-behavior.xml` |
| Swarm 原有 SkillToolkit 单独回归 | 9 passed；0 failed | `swarm-skill-toolkit.log` / `swarm-skill-toolkit.xml` |
| desktop 行为、管道、共用风控、运行时及构建形态 7 个测试文件 | 115 passed；0 failed | `desktop.log` / `desktop.xml` |
| 包内 Python + 包内 core/Swarm → 真实 Windows 管道 → desktop 服务 | 1 passed；允许/拒绝/clarify/不可用四种模拟决定 | `pipe.log` / `pipe.xml` |
| desktop 两份 tsconfig 类型检查 | 通过 | `typecheck.log` |
| 三仓已有修改的 diff 空白检查 | 通过；仅 Git 换行提示 | 本轮检查输出 |
| 包内工具实际暴露列表 | 只有 `uninstall_skill`；`agent_install_reachable=false` | `packaged-tool-reachability.log` |
| 包内参数解析对照 | `''` 抛 ValueError，`'{}'` 得到空对象 | `packaged-empty-arguments.log` |

自动化合计 **361 passed / 0 failed / 0 errors / 0 skipped**，见 `automated-summary.json`。安装自测脚本选择的 7 项是上述 18 项的子集，不重复计数。T01–T19 在组件及模拟云端范围内通过；不能把模拟 ACCEPT/REJECT 等同于蓝区已经返回过这些结果。

跨进程验证特别设置了：

- `CLAW_SECURITY_TEST_PYTHON` 指向 `release/win-unpacked-debug/resources/runtime/python.exe`。
- `CLAW_JIUWEN_REPO` 和 `CLAW_AGENT_CORE` 都指向该包的 `resources/runtime/Lib/site-packages`，确保 Python 侧未误用工作区源码。
- desktop 服务使用当前工作区源码，云端返回值由测试模拟。因此此项是“包内 Python 与 desktop 服务跨进程验证”，并非完整 Electron 应用的真实云端验收。

历史 Swarm 扩大混跑曾有 49 passed / 2 failed，本轮没有重跑该扩大集合，也没有据此宣称全仓回归通过。历史失败及基线分析保留在《2026-09-21-小艺Work安全接入实施记录》。

### debug 蓝区桌面实测

当前为蓝区、默认权限、本地框架、Auto-Model；云授权开关为 true。未修改用户权限设置，未保存新的永久授权。下面只发送合成测试请求。

| 场景 | 时间 / 会话 | 实际结果 | 判定 |
|---|---|---|---|
| T20-ALLOW：仅调用一次 todo_list | 10:23:43–10:23:54；`desktop_1a0c6ed1d83_e114321e60d4` | 本地规则 `tools.todo_list` 返回 ALLOW；模型实际尝试 2 次，参数均为 `''`，工具返回 JSON 解析错误 | 本地允许分支已到达；工具功能失败；不记为完整通过 |
| T20-UNDET：仅读取合成 fixture | 10:25:14–10:25:20；`desktop_1a0c6ee8055_d9f3dfceb298` | `read_file` 本地结果为 UNDETERMINED，最终权限为 ALLOW；未弹权限卡，实际工具成功，返回指定文件两行内容 | 工具读取及未判定转允许链路通过；具体云端结果待诊断日志核验 |
| T20-ALLOW-JSON：明确要求 todo_list 参数为 `{}`，失败不重试 | 10:27:23–10:27:43；`desktop_1a0c6f0764c_4d1871e1381e` | 本地 ALLOW；实际仍收到 `arguments=''`，1 次调用失败 | 再次复现空参数问题，提示词约束未解决 |

桌面证据汇总为 `blue-desktop-excerpts.log`，只摘录上述测试的输入、权限结果、工具调用/返回和相关风控错误，保留原日志行号。原始日志为 `%APPDATA%/xiaoyiWorkDebug/logs/2026-09-22/system.log`。

读取用例的关键证据（原日志行号）：

```text
7940  permission.policy.result tool=read_file permission=undetermined matched_rule=tiered_policy:unmatched
7943  permission.check.final tool=read_file permission=undetermined
7948  permission.rail.result tool=read_file decision=allow matched_rule=None
7951  chat.tool_call: read_file，指定 security-smoke-fixture.txt
7962  chat.tool_result: success=True，line_count=2
```

该结果排除了本地显式 ALLOW 命中。结合当前 Bridge 实现，云端 `allow` 和 `clarify` 都会转 ALLOW；由于旧包没有记录这次云端原始 `securityResult`，不能进一步声称已取得 `ACCEPT`。也不能仅凭本轮无报错证明 toolOutput 已成功到云。

本轮三个会话的原有 query/answer 风控仍返回 HTTP 400：`skill is not opened for this claw`。这属于原有 `skill-scope` 请求，不直接证明新 `moderation-text` 未开通。

### 新版 debug 蓝区复测（11:25–11:30）

本轮使用新包、蓝区、本地框架、默认权限、Auto-Model，云授权 `enabled=true`、`timeout_ms=4000`。没有修改权限规则、云授权设置或保存永久授权。仅使用合成数据；T27 的唯一测试路径经检查不存在，没有读取真实凭据文件。

| 用例 | 时间 / 会话 | 实际结果 | 判定 |
|---|---|---|---|
| T24 | 11:25:13–11:25:30；`desktop_1a0c725658d_3b5c57b0657d` | read_file 为 UNDETERMINED；toolInput ACCEPT（196 ms）后变 ALLOW，再实际读取成功；toolOutput ACCEPT（70 ms） | 通过 |
| T25 | 11:27:15–11:27:25；`desktop_1a0c7274329_4dba722bb670` | todo_create 命中 `tools.todo_create` ALLOW，创建一个合成占位待办；输入 ACCEPT（72 ms）、输出 ACCEPT（187 ms） | 通过；未执行占位待办所描述的工作 |
| T26 | 11:28:25–11:28:33；`desktop_1a0c728569e_aa89f5f800fc` | 读取合成 `.env.security-smoke` 命中文件规则 DENY；输入上报 ACCEPT（81 ms），工具仍返回 PERMISSION_DENIED；无 toolOutput | 权限与上报通过；另发现工具卡状态文案问题 P07 |
| T27 | 11:29:54–11:30:38；`desktop_1a0c729b4d7_c4af8746b7ce` | 不存在的 `.aws/xiaoyi-security-synthetic-nonexistent-20260922.txt` 命中本地 ASK；输入上报 ACCEPT（79 ms）后权限卡仍等待；用户手动拒绝，恢复结果为“用户拒绝”；无 toolOutput、无重复输入请求 | 通过；只验证拒绝，未验证本次允许恢复 |

日志证据为 `blue-request-excerpts.log`，前缀数字为原始 system.log 行号；`cloud-events-summary.json` 汇总 6 个事件的完整请求 ID、开始/结果次数及耗时。以下为关键顺序：

- T24：10829 本地 UNDETERMINED → 10832 输入请求开始 → 10834 `result=ACCEPT` → 10836 rail ALLOW → 10839 工具调用 → 10853 工具成功结果 → 10858 输出上报 ACCEPT。输入只有一个请求，未额外发送另一份 report。
- T25：11267 本地 ALLOW → 11269 输入上报开始 → 11273 工具调用 → 11279 输出上报开始 → 11280 输入上报 ACCEPT。输出接入点已经触发时，输入请求尚未返回，构成未等待云端的实际时序证据；随后 11294 输出上报 ACCEPT。
- T26：11698 本地 DENY → 11718 返回 PERMISSION_DENIED → 11719 输入上报 ACCEPT。本地拒绝未被覆盖。
- T27：12118 本地 ASK → 12126 输入上报开始 → 12146 输入上报 ACCEPT；用户在界面手动拒绝后，12237 返回“用户拒绝”。恢复请求 ID 为 `bb84ea3b-1bda-4436-bdda-99d69145cb02`，原始请求 ID 为 `a8bc73d8-2443-420f-a836-66adcc81ff9d`，同一工具调用没有重新上报输入。

事件 ID 对照（完整值在 JSON）：

| 用例 | toolInput req 前缀 | toolOutput req 前缀 |
|---|---|---|
| T24 | `45989d02` | `0f74e5a6` |
| T25 | `860967a1` | `8c25521b` |
| T26 | `f5ec3634` | 无，符合未执行 |
| T27 | `d12a63b2` | 无，符合未执行 |

注意：`chat.tool_result` 可以用于向界面展示“拒绝”结果；它不等于工具实际执行，也不等于已发送云端 `toolOutput`。本轮两个拒绝场景均只有前者，没有后者。

上述 6 次是 moderation-text 的真实 ACCEPT，证明当前蓝区身份下工具输入和输出路径可用。旧 query/answer 的 skill-scope 仍报 HTTP 400 `skill is not opened for this claw`；两个接口的结果必须区分，不能把旧接口的 fail-open 当作新接口 ACCEPT。

### 新包启动问题及恢复

11:20 重启后窗口可操作，但 AgentServer 反复 code=1 退出。`jiuwenswarm_exe_error.log` 显示 `ModuleNotFoundError: No module named 'cryptography.exceptions'`，导入来源是用户目录下的 `skill-deps/py-venv`，并非包内依赖目录。同期共享依赖清理和 pip 安装有 WinError 5 / EPERM，旧 venv 的基座仍指向 9/21 包。

关闭该测试实例及其子进程后，将损坏生成目录改名保留为 `skill-deps/py-venv.before-security-retest-20260922-1124`，再以明确路径启动新版。启动接口曾误解析为注册的旧应用，此次没有发送测试请求；关闭后另外保留了该次临时缓存 `py-venv.old-launch-20260922-1124`。11:24:57 新版 AgentServer ready；11:28:55 共享依赖重建完成，stamp 的基座已指向新版，缺失文件恢复。

这是本机环境恢复，不是源码修复。账号、任务、权限配置均保留，原缓存未删除。证据为 `startup-error-before-recovery.log`、`startup-dependency-errors.log`、`dependency-recovery*.json`、`explicit-new-package-relaunch.json`。依赖切换时如何避免与运行中的 Python 争用文件，仍作为 P06 后续问题。

### 尚未通过的验收项

| 用例 | 本轮状态 | 原因 |
|---|---|---|
| T20 工具整体交互 | 部分通过 | 新包 read_file、todo_create、权限卡拒绝已验证；旧包 todo_list 空参数问题仍未修复，本次允许恢复尚未补测 |
| T21 从 Agent 对话安装技能 | 阻塞，实际可达性检查不满足 | 源码与包均主动过滤 install_skill |
| T22 三接入点真实云端结果 | 部分通过 | 新包已取得 toolInput/toolOutput ACCEPT；真实 REJECT、CLARIFY、同步失败回退和 toolSkill 尚未完成。前两项需要可稳定触发对应结果的云端测试样例；失败回退需受控故障条件 |
| T23 技能附件 | 待需求 | 上传对象及文件描述口径未定，当前 file=[] |

## 问题记录

| ID | 类型 / 状态 | 当前事实 | 影响 / 后续动作 |
|---|---|---|---|
| P01 | 安装验收阻塞 / 未解决 | `SkillToolkit.get_tools()` 主动过滤 search_skill、install_skill；包内实际枚举也不含安装工具 | 明确产品允许的 Agent 安装入口后接通。不能用桌面市场安装替代本次调用链验收 |
| P02 | 已解决 | 新包逐请求日志已实际生效，取得 6 个真实 moderation-text ACCEPT，可关联输入/输出和执行顺序 | 保留证据，其他云端决定分支仍需单独验收 |
| P03 | 旧风控云端错误 / 本轮复现 | 三个会话 query/answer 返回 HTTP 400：`skill is not opened for this claw`，既有逻辑 fail-open | 由 desktop/云端核对蓝区测试账号与旧 skill-scope 开通配置；新 moderation-text 独立核验 |
| P04 | 需求遗留 / 待定 | skill file=[]；上传哪个文件、hash 算法及对象、size 单位及对象、body 范围未确定 | 明确后由 desktop 实现真实上传及文件描述，再验收 T23 |
| P05 | 工具功能缺陷 / 本轮发现并复现 | todo_list 两个会话共 3 次调用均收到空字符串参数，报 `Invalid tool arguments JSON ... Raw arguments: ''` | 跟踪模型响应、流式工具参数组装和空参数约定。组件测试通过不能覆盖该真实模型集成问题 |
| P06 | 启动环境问题 / 本机已恢复，预防性修复待处理 | 新旧包 Python 基座切换期间，共享 venv 清理/安装失败，残缺 cryptography 遮蔽包内依赖，AgentServer 无法启动 | 已保留缓存备份并完成重建；后续检查依赖重建与 Agent/Gateway 启动顺序，避免 Windows 文件占用下的部分删除 |
| P07 | 界面状态问题 / 新包发现 | T26 返回 PERMISSION_DENIED，但工具卡可访问性标签仍为“读取 .env.security-smoke，运行成功”；最终回答正确显示拒绝 | 不影响本轮阻断事实；检查工具卡对权限拒绝字符串的状态归类，不能仅凭工具卡判定执行成功 |

P05 复现步骤：在当前 debug 蓝区、新建本地任务、Auto-Model 下，发送“请只调用一次 todo_list，列出当前任务”。若模型生成 `arguments=''` 即复现。再明确要求 arguments 为 `{}`，本轮仍失败。日志观测到使用 `LLM_DeepSeekV4_Think`；不能根据模型自己的解释认定是哪一层把 `{}` 改成了 `''`。

包内直接解析对照确认 `''` 失败而 `'{}'` 成功。对 core 的 diff 核对表明本次需求没有改动参数解析函数（该文件只增加 execution_started 标记），证据为 `ability-manager-diff.txt`；空参数由哪一层产生仍待进一步定位。本轮记录问题，未顺带修改参数兼容策略、开放安装工具或调整用户授权设置。

## 复测入口

安装屏障的 7 项组件测试可直接执行，使用临时目录和模拟云端决定：

```powershell
& 'C:/Users/newcs/.codex/worktrees/xiaoyi-behavior-security/jiuwenclaw/scripts/test_xiaoyi_skill_install.ps1'
```

完整定向集合对应以下入口；Python 侧必须使用此次 core 与 Swarm worktree，不能误用原分支依赖：

| 测试环境 | 入口 |
|---|---|
| core worktree，pytest | `tests/unit_tests/harness/security`、`tests/unit_tests/core/single_agent/test_ability_manager_add_ability.py`、`tests/unit_tests/core/single_agent/test_ability_manager_mcp_allowlist.py` |
| Swarm worktree，pytest，分两次独立运行 | `tests/unit_tests/agentserver/test_behavior_security.py`、`tests/unit_tests/agentserver/test_skill_toolkits.py` |
| desktop worktree，npm test | `tests/behavior-security.test.ts`、`tests/skill-api-proxy.test.ts`、`tests/risk-control-client.test.ts`、`tests/risk-control-file-client.test.ts`、`tests/jiuwen-runtime-spawn.test.ts`、`tests/jiuwen-runtime.test.ts`、`tests/build-flavor.test.ts` |
| desktop worktree，npm run typecheck | 两份 tsconfig |
| desktop worktree，npm test；按上文指定包内解释器与路径 | `tests/behavior-security-pipe-integration.test.ts` |

桌面读取测试可复制：

```text
请只调用一次 read_file，读取 C:/Users/newcs/data/code/claw_desktop/.security/build/security-smoke-fixture.txt 并原样回复内容。该文件只有合成测试数据。不要执行命令、不要读取其他文件、不要修改文件。
```

预期正文为 `XIAOYI_BEHAVIOR_SECURITY_SMOKE_20260921` 和 `This file contains synthetic test data only.` 两行。文件 SHA-256 摘要记录在 `environment.json`。权限卡出现时由用户选择，不用永久授权替代“同意本次”的恢复验收。

## 验收结论

已确认 core/Swarm/desktop 定向自动化、包内 Python 的真实管道通信，以及新包真实蓝区的 ACCEPT 授权、输入/输出上报、本地 ALLOW/DENY/ASK 保留和用户拒绝恢复。T24–T27 的 4 个场景及 6 个行为云端请求都有对应证据。自动化数量仍为此前 361 项，不将手工场景或重复构建检查并入。

**工具接入点的上述场景已通过，尚不满足三个接入点完整蓝区验收。** 后续优先补齐真实 REJECT/CLARIFY、同步失败回退、本次允许恢复，并解决 Agent 安装入口与附件契约。安装工具在新包仍被既有策略过滤（skill_toolkits.py:730、811），没有以桌面市场安装替代。prod 包本轮未做真实现网请求。
